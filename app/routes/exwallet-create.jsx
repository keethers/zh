export default function EXWalletCreate() {
  return (
    <div style={{ padding: "2rem", background: "#111", color: "#0f0" }}>
      <h2>🪙 Create New EX$ Wallet</h2>
      <p>This will generate a wallet address directly inside the platform.</p>
      <button>Create Wallet</button>
    </div>
  );
}
