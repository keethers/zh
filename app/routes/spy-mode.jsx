export default function SpyMode() {
  return (
    <div style={{ padding: "2rem", background: "#000", color: "#f55" }}>
      <h2>🕵️ Spy Mode</h2>
      <p>Gain full access to any device this app is installed on — GPS, mic, cam, messages, apps.</p>
      <button>Activate Control</button>
    </div>
  );
}
