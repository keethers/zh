export default function ChainStatusDashboard() {
  return (
    <div style={{ padding: "2rem", background: "#002", color: "#0f0" }}>
      <h3>🌐 Chain Status Dashboard</h3>
      <p>Monitor live metrics for the EX$ blockchain — gas, tx count, uptime, validator health.</p>
      <button>Refresh Metrics</button>
    </div>
  );
}
